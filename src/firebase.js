// firebase.js — Firebase configuration for Google Auth
// IMPORTANT: Replace these with your actual Firebase project credentials
// Go to https://console.firebase.google.com → Create/Select Project → Authentication → Enable Google Sign-In
// Then go to Project Settings → Your Apps → Web App → Config

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

export default firebaseConfig;
